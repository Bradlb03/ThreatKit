# ThreatKit
1. To start flask, I opened files in VSCode
2. in terminal paste "docker build -t flask-app" and hit enter, paste "docker run -p 5000:5000 flask-app" and hit enter.
3. then with app.py file selected click the play button drop down in top right and click run python file.
4. In terminal a IP address should open in it and you should be able to copy and paste that into the search bar of google etc.

cd ThreatKit
python -m venv .venv

NEEDED FOR PASSWORD Ai TEST
pip install zxcvbn

 macOS/Linux:
source .venv/bin/activate

Windows PowerShell:
.venv\Scripts\Activate.ps1

then run
pip install -r requirements.txt
python app.py
